description: ZMK Device Detector

compatible: "zmk,device-detector"

include: base.yaml

properties:
  sampling-hz:
    type: int
    default: 2
    description: Sampling frequency in Hz

  io-channels:
    type: phandle-array
    required: true
    description: ADC channel for device detection
